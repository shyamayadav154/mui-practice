import { createTheme } from '@mui/material';

export default createTheme({
  palette: {
    primary: {
      main: '#5784BA',
      light: '#F7F6CF',
    },
    secondary: {
      main: '#F4CFDF',
    },
    otherColor: {
      main: '#A57283',
    },
  },
});
